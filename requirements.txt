# Market Research PoC - minimal deps for v0.1 product lines slice
httpx>=0.27
openai>=1.30
duckduckgo-search>=6.0   # fallback search (no key)
python-dotenv>=1.0
rich>=13.0               # pretty CLI tables + progress
streamlit>=1.35          # basic interactive UI
typer>=0.12              # nice CLI (or use argparse)
# Optional but recommended for better page text extraction:
# trafilatura>=1.9
